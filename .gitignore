# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.vercel

# Source PNGs for the Root Guardian preview gallery — only the optimized
# WebPs (also in public/preview) are committed and shipped. The PNGs are
# reproducible by re-running scripts/optimize-preview.mjs from a fresh
# pull of the originals.
public/preview/*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
